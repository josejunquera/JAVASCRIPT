'use strict';

const matrix = [
  ['*', '*', '*'],
  ['*', '*', '*'],
  ['*', '*', '*'],
];

console.log(matrix);

/*Primer array secundario siempre primera posición simbolo
  Cada linea siguiente +1 en la posición para insertar simbolo.

*/

function insertSymbol(array) {
  for (let i = 0; i < array.length; i++) {
    // console.log(i);
  }
}
insertSymbol(matrix);
